fix: volver al comando EXACTO original de yugabyted (sin tserver_flags)

El flag --tserver_flags causaba crash de yugabyted con Python TypeError:
  ProcessManager.wait_until_stop() missing 1 required positional argument: 'pid'

Solucion: volver al comando original que SI funcionaba:
  bin/yugabyted start --base_dir=/mnt/master --daemon=false

Sin --tserver_flags, sin --background=false, sin --advertise_address.

El limite de tabletas se maneja ahora con SPLIT INTO 1 TABLETS
en las tablas nuevas (076, 077, 078). Cada tabla usa solo 1 tableta
en vez de 8-16, reduciendo drasticamente el consumo de tabletas
sin necesidad de cambiar flags del tserver.

Migraciones con SPLIT INTO 1 TABLETS:
- 076: federation_constants, federation_proposals, federation_votes
- 077: federation_expelled_nodes, federation_known_nodes
- 078: cluster_config

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
