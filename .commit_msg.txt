fix: pasaporte como documento separado + sin membresia dual, combinar saldos

Cambios solicitados por el usuario:

1. Pasaporte es documento SEPARADO del ID nacional:
   - users.passport_number, passport_country (nueva migracion 063)
   - admission_requests.passport_number, passport_country
   - User struct, CreateUser, ApproveAdmissionRequest, updateMyContacts
   - Frontend: seccion separada en Profile y Admission
   - Validacion: puede registrarse con ID nacional, pasaporte, o ambos
   - No todos tienen pasaporte, pero los que si pueden validarse por el

2. No membresia dual - eliminar opcion "both":
   - Solo 'a' o 'b' (el usuario se queda en UN nodo)
   - No se puede estar en dos nodos a la vez

3. Combinar saldos (suma algebraica) en vez de "transfer":
   - combine: +10 y -20 = -10 (se compensan)
   - combine: +10 y +5 = +15 (mas positivo)
   - combine: -10 y -20 = -30 (mas negativo)
   - forgive_debt: condona deuda del nodo removido
   - remove_balance: descarta saldo del nodo removido

4. Deteccion de conflictos por national_id OR passport:
   - scan-conflicts busca coincidencias por cedula O pasaporte
   - match_type: 'national_id', 'passport', o 'both'
   - Frontend muestra que tipo de coincidencia se detecto

Generated with Devin

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
