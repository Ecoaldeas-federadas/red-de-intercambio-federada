fix: validar que dominio y nombre del nodo no queden vacios al crear

El config.yaml ahora tiene domain y name vacios (plantilla), pero al
crear el nodo el setup debe garantizar que se guarden valores reales.

Fix en setup.go (initNode):
- Agregada validacion: si nodeDomain queda vacio despues del fallback
  (req.NodeDomain -> sh.NodeDomain), retorna 400 con mensaje claro
- Agregada validacion: si nodeName queda vacio, retorna 400
- Mensajes explican que el dominio es obligatorio (localhost para
  desarrollo o dominio real para produccion)

El frontend (Setup.tsx) ya validaba estos campos antes de enviar,
pero el backend no tenia la validacion. Ahora ambos la tienen.

Generated with Devin

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
