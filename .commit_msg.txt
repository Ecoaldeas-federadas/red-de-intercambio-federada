feat: documentos multiples + paises del mundo en dropdown

Cambios solicitados por el usuario:

1. Documentos multiples (no solo uno):
   - Nueva tabla user_documents (un usuario puede tener varios documentos)
   - Tipos: cedula, DNI, pasaporte, RUT, CURP, carnet de conducir, etc.
   - Cada documento tiene: tipo, numero, pais emisor
   - Comparacion entre nodos se hace por tipo + numero (mismo tipo)
   - CRUD completo: GET/POST/DELETE /api/auth/me/documents

2. Paises del mundo en dropdown (no texto libre):
   - Nueva tabla countries con ~195 paises (ISO 3166-1)
   - Endpoint GET /api/countries devuelve todos los paises
   - Frontend usa dropdown con todos los paises
   - Admin puede agregar pais nuevo: POST /api/countries

3. Tipos de documento en dropdown:
   - Nueva tabla document_types
   - Endpoint GET /api/document-types
   - Frontend usa dropdown con tipos disponibles

4. Admission con documentos multiples:
   - El solicitante agrega varios documentos antes de enviar
   - Al aprobar, los documentos se transfieren a user_documents
   - Tabla admission_documents para documentos pendientes

5. Deteccion de conflictos usa user_documents:
   - scan-conflicts busca por tipo + numero en user_documents
   - Compara cedula con cedula, pasaporte con pasaporte, etc.

Generated with Devin

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
