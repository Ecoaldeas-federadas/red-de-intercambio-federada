feat: update.ps1 auto-actualizable + arranca updater-controller

MEJORAS A update.ps1:
- Auto-actualizacion: si el propio script cambia tras git pull, se
  re-ejecuta con la nueva version (el usuario nunca necesita hacer nada extra)
- Limpieza de contenedores huerfanos de actualizaciones fallidas
  (contenedores sin nombre o con sufijo _red-de-intercambio-federada-)
- Detiene demo-app si quedo corriendo por error (debe arrancarse desde la web)
- Construye updater-controller (nuevo servicio)
- Arranca TODOS los servicios que deben estar siempre corriendo:
  yugabytedb, db-backup, demo-controller, demo-stopper, updater-controller, node-app
- NO arranca demo-app (se arranca desde la web con un boton)
- Verifica que Docker Desktop este respondiendo antes de continuar
- Muestra el commit actual al final
- Tiempo de espera del servidor aumentado a 90s (era 60s)

MEJORAS A install.ps1:
- Construye updater-controller junto con node-app
- Arranca todos los servicios excepto demo-app

El usuario solo necesita correr:
  powershell -ExecutionPolicy Bypass -File update.ps1

Y todo se actualiza: codigo, imagenes, servicios nuevos, limpieza.
Si el propio update.ps1 cambio, se re-ejecuta solo con la nueva version.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
