fix: Eliminar todas las URLs de Unsplash (499 URLs) + assetUrl en tienda

## Problema
La tienda y la lista de productos mostraban URLs de images.unsplash.com
que devuelven 404. El nodo demo hereda productos de las migraciones
(node_domain='localhost'/'default') que sobreviven el DemoReset porque
este solo borra node_domain='demo'.

## Solucion

### 1. Reemplazar 499 URLs de Unsplash en seed.go y migraciones
- internal/db/seed.go: 255 URLs
- 13 archivos de migraciones: 244 URLs
- Todas reemplazadas con /placeholder.svg

### 2. Nueva migracion 067_replace_unsplash_urls.sql
- UPDATE products SET image_url = '/placeholder.svg' WHERE image_url
  LIKE '%unsplash%'
- Tambien limpia store_items, public_settings, public_pages
- Esto arregla las bases de datos existentes que ya tienen los
  productos con URLs de Unsplash

### 3. assetUrl() en Store.tsx y Products.tsx
- Las paginas de tienda y productos ahora usan assetUrl() para
  resolver las rutas de imagenes segun el basePath
- Nodo normal: /placeholder.svg -> localhost:8080/placeholder.svg
- Nodo demo: /placeholder.svg -> localhost:9091/demo/placeholder.svg

### Resultado
- No mas errores 404 de images.unsplash.com en la consola
- No mas errores 404 de /placeholder.svg en el nodo demo
- Cada nodo carga sus assets desde su propia raiz

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
