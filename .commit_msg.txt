fix: Limpiar advertencias restantes + logs detallados en actualizacion

1. Advertencias del linter resueltas:
   - external.go: usar indice en range en vez de copia para que
     la escritura a ComponentUnit sea visible para el linter
   - connection.go: usar filename y stmtNum en mensaje de error
     para que no sean parametros sin uso

2. Logs detallados en actualizacion del nodo:
   - Cada paso del update ahora loguea que esta haciendo
   - Muestra el remote URL despues de configurar el token
   - Muestra el output completo de git fetch, stash, reset, build
   - Muestra el nuevo commit despues del reset
   - El log se muestra en la UI con consola oscura (fondo negro)
   - El log se muestra en running, error y completed
   - Altura mayor (max-h-60) para ver mas contenido

3. Importante: el error "Error en git pull" que ves en la pagina
   es del codigo VIEJO corriendo en el nodo. El codigo nuevo usa
   git fetch + git reset --hard (no git pull). Para aplicar este
   fix necesitas actualizar manualmente una vez mas:
     git pull origin main
     docker compose build node-app
     docker compose up -d node-app

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
