feat: pagina publica de gobernanza /p/gobernanza - Ley de la Aldea

El sistema de gobernanza ya existia en el backend (migracion 048 con
reglas pre-aprobadas, endpoints /api/public/governance, integracion
con asamblea) y en el panel admin (Governance.tsx), pero faltaba
mostrarlo en la pagina publica.

Nuevo componente PublicGovernancePage.tsx:
- Carga reglas desde /api/public/governance (endpoint publico)
- Agrupa por categoria con iconos y colores
- Navegacion por categorias con scroll suave
- Muestra severidad (info, leve, grave, muy grave)
- Diseno responsive con tarjetas
- Footer con instruccion de como proponer modificaciones

Integracion en PublicSite.tsx:
- PublicPageView: cuando slug === 'gobernanza', renderiza
  PublicGovernancePage en vez de bloques editables
- Agregada pagina virtual 'gobernanza' al menu publico (menu_order 90)
- Agregada a communityPages en la navegacion

Fix en system.go (listGovernanceRules):
- Antes filtraba por node_domain = 'localhost' hardcoded
- Ahora usa el dominio del header o del nodo, con fallback a localhost
- Busca en node_domain IN (dominio, 'localhost', 'default') para
  incluir reglas seed

Las reglas se muestran:
1. Pagina publica /p/gobernanza (cualquier visitante)
2. Formulario de admision (aceptacion obligatoria)
3. Panel admin /app/governance (gestion)

Cuando la asamblea aprueba un cambio (crear, modificar, eliminar
regla), se refleja automaticamente en la pagina publica porque lee
directamente de la tabla governance_rules.

Generated with Devin

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
