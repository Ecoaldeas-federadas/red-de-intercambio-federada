fix: login con clave invalido - passwordLogin usaba NodeDomain vacio

El passwordLogin buscaba al usuario con WHERE u.node_domain = $2
pasando ah.NodeDomain, pero como el config.yaml ahora tiene el
dominio vacio, ah.NodeDomain es "" y la query no encontraba al
usuario (su dominio real es "localhost").

Fix: el passwordLogin ahora busca al usuario solo por username
(sin filtrar por node_domain) y obtiene el node_domain real del
usuario desde la BD para usarlo en el token JWT.

Esto es consistente con como funciona el login por passkey, que
tambien obtiene el node_domain del usuario desde la BD.

Generated with Devin

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
