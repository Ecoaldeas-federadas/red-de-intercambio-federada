fix: configuracion de aprobaciones vacia en demo - separar de demoSeedBoardMembers

PROBLEMA:
La pestana "Configuracion de Aprobaciones" de la Asamblea estaba vacia
en el nodo demo. La causa era que la insercion de assembly_config
estaba dentro de demoSeedBoardMembers, que tiene un early return si
ya existen board_members. Si la funcion retornaba temprano, la
configuracion de aprobaciones nunca se insertaba.

SOLUCION:
- Extraida la logica de assembly_config, quorum_config y tax_config
  a una nueva funcion demoSeedAssemblyConfig que se llama
  independientemente desde el orquestador principal
- demoSeedBoardMembers ahora solo inserta board_members y quorum
- demoSeedAssemblyConfig se ejecuta siempre, incluso si
  demoSeedBoardMembers retorna temprano
- Agregados los nuevos tipos de propuesta (product_import,
  product_remove, product_to_base, governance_rule) a la config
- Corregido tax_config: ahora usa ON CONFLICT (node_domain) DO UPDATE
  porque tax_config tiene UNIQUE en node_domain (antes intentaba
  insertar 8 filas con el mismo dominio y solo la primera funcionaba)

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
