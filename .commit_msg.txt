fix: Admision muestra datos + EntitySelector usa api.get

## Problemas
1. Admision en blanco: frontend usaba u.username pero backend devuelve
   u.proposed_username. Lo mismo con requested_credit_limit que no existe.
2. GET /products 404: EntitySelector usaba fetch(endpoint) sin prefijo /api
3. Las solicitudes aprobadas/rechazadas no se mostraban

## Solucion
1. Admission.tsx:
   - Usa proposed_username, display_name, proposed_level, status
   - Muestra badge de estado: Aprobada/Rechazada/Pendiente
   - Muestra nivel solicitado y fecha de solicitud
   - Botones Aprobar/Rechazar solo si status === 'pending'
   - Muestra todas las solicitudes (no solo pendientes)

2. EntitySelector.tsx:
   - Usa api.get() en vez de fetch() directo
   - Agrega soporte para data.products

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
