fix: startDemoNode usaba ruta relativa, no encontraba docker-compose.yml

PROBLEMA RAIZ:
El startDemoNode (boton "Arrancar demo" en la web) usaba os.Getenv("PROJECT_DIR")
que NO esta definido en el docker-compose.yml del node-app. Por eso usaba
"docker-compose.yml" (ruta relativa), que se resuelve a /app/docker-compose.yml
(que no existe). El repo esta montado en /project, no en /app.

Resultado: el boton "Arrancar demo" NUNCA funcionaba desde dentro del contenedor
porque no encontraba el docker-compose.yml. El demo-app nunca se creaba.

SOLUCION:
- startDemoNode: usar /project/docker-compose.yml directamente (no PROJECT_DIR)
- getDemoStatus: usar detectComposeProjectName() para el nombre del contenedor
- update.ps1: NO crear demo-app en el script (solo construir la imagen).
  El demo-app se crea solo al pulsar "Arrancar demo" en la web.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
