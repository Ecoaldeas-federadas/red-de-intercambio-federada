feat: nodo demo integrado en docker-compose + login con botones de roles

El nodo demo ahora se instala y actualiza solo con el nodo principal.

## Cambios principales

1. **Integrado en docker-compose.yml** (no mas docker-compose.demo.yml separado):
   - demo-app: puerto 9091 (API) / 9044 (federacion)
   - demo-resetter: borra y re-seedea cada 24h
   - BD separada: fmc_demo (no toca fmc_node)
   - Se levanta con `docker compose up -d` (junto con el nodo principal)

2. **Seed demo rico** (no nodo vacio):
   - Super Admin (demo)
   - Junta Directiva: presidente, vicepresidente, tesorero, secretario, 2 vocales
   - 6 miembros activos: agricultora, productor, artesano, panadera, mecanico, maestra
   - 1 miembro nuevo
   - 5 organizaciones: cooperativa agricola, panaderia, taller mecanico, tienda de arte, centro de salud
   - 6 departamentos: Junta Directiva, Economia, Educacion, Salud, Ambiente, Admision
   - ~18 productos demo genericos
   - Paginas publicas genericas (no Feria Conuquera)

3. **Login con botones de roles** (no usuario/clave):
   - El nodo demo detecta automaticamente que es demo
   - Muestra botones agrupados por rol:
     - Super Admin (corona ambar)
     - Junta Directiva (usuarios azul)
     - Organizaciones (edificio purpura)
     - Miembros (circulo verde)
   - Un click entra directamente con ese rol
   - Password: demo1234 para todos

4. **Reset manual desde el admin**:
   - Nueva pestana "Nodo Demo" en Configuracion
   - Boton "Resetear Nodo Demo" que reinicia el contenedor
   - El nodo demo hace auto-setup + seed al arrancar
   - Util cuando hay actualizaciones del sistema

5. **Puertos diferentes** (no chocan con desarrollo):
   - 9091 en vez de 8081
   - 9044 en vez de 8444

6. **API nueva**:
   - GET /api/demo/users: lista usuarios demo para botones de login
   - POST /api/demo/login: login con username + password especificos
   - POST /api/admin/demo/reset: resetear nodo demo (admin)

Generated with Devin

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
