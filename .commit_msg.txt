fix: votos de asamblea demo + traducir estados a español

PROBLEMA 1: Votos de asamblea demo no se insertaban
- demoSeedAssemblyVotes buscaba decisiones con:
  SELECT id FROM assembly_decisions WHERE status = 'approved'
  Sin filtrar por node_domain (assembly_decisions no tiene node_domain)
- Fix: JOIN con assembly_sessions para filtrar por node_domain:
  SELECT d.id FROM assembly_decisions d
  JOIN assembly_sessions s ON d.assembly_id = s.id
  WHERE s.node_domain = $1 AND d.status = 'approved'
- Agregado logging para diagnosticar

PROBLEMA 2: Estados de asamblea en inglés
- approved, rejected, executed, proposed, expired se mostraban en inglés
- Fix: traducidos en Assembly.tsx
  approved -> aprobada
  rejected -> rechazada
  executed -> ejecutada
  proposed -> pendiente de revision
  expired -> vencida
- result tambien traducido: approved -> aprobada, rejected -> rechazada

PROBLEMA 3: Tipos de propuesta sin traducir
- budget, election, product_approval, federation no estaban en PROPOSAL_LABELS
- Fix: agregados al diccionario
  budget -> Presupuesto
  election -> Eleccion
  product_approval -> Aprobacion de producto
  federation -> Federacion
- PROPOSAL_LABELS cambiado de Record<ProposalType, string> a Record<string, string>
  para soportar tipos adicionales sin errores de TypeScript

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
