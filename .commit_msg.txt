fix: preferencias de notificaciones - solo canales configurados + select all

Mejoras de UX en la pagina de preferencias de notificaciones:

1. Solo mostrar canales configurados:
   - El backend ahora devuelve gateway_active en /notifications/channels
   - El frontend filtra y solo muestra canales con gateway_active = true
   - Si no hay ninguna pasarela configurada, muestra mensaje informativo
   - Los usuarios ya no ven opciones de canales no configurados

2. Select all masivo:
   - Checkbox "Todos" en cada columna (activar todos los eventos para un canal)
   - Checkbox en cada fila (activar todos los canales para un evento)
   - Estado indeterminate cuando algunos estan activados
   - Botones "Activar todo" y "Desactivar todo" globales
   - Ya no hay que tildar uno por uno

3. Verificacion de permisos:
   - La pestana "Pasarelas (admin)" solo es visible con permiso config.manage
   - Los usuarios sin permiso no ven la configuracion de pasarelas

Generated with Devin

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
