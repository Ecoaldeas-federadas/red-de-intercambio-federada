fix: migracion 061 - columna metadata en users (causaba 404 en /auth/me)

Problema: GetUser consulta COALESCE(metadata, '{}'::jsonb) pero la columna
metadata no existia en la tabla users. Esto hacia que la query fallara y
getMe retornaba 404 "user not found" para TODOS los usuarios.

Solucion: migracion 061_user_metadata.sql anade la columna metadata JSONB
con default '{}' a la tabla users.

Tambien afectaba:
- updateMyContacts (guardar digest_mode en metadata)
- notification_scheduler (leer digest_mode y last_digest_sent)

Generated with Devin

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
