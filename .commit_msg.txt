feat: Separar padre en /main + demo en /demo + sesiones separadas + fixes

CAMBIOS PRINCIPALES:

1. Padre se sirve bajo /main (antes en raiz /)
   - La raiz / sirve un HTML estatico de redirect (NO el SPA)
   - El navegador carga una pagina real antes de navegar a /main
   - Esto separa completamente las aplicaciones padre y demo

2. Demo usa __LOCAL__ para datos (igual que el padre)
   - BD separada fmc_demo, __LOCAL__ es independiente
   - node_config.node_domain = {dominio_del_padre}/demo
   - DemoReset borra datos por __LOCAL__, node_config por dominio real
   - DemoSeedData usa __LOCAL__ para todos los datos
   - One-time cleanup: borra datos viejos con node_domain='demo'
   - Fix: usuarios duplicados ya no ocurren

3. Demo hereda dominio del padre automaticamente
   - startDemoNode escribe dominio del padre en .demo-shared/parent-domain.txt
   - Demo lee ese archivo al arrancar
   - Si el padre cambia de dominio, el demo se actualiza al reiniciar
   - Settings del demo: dominio read-only

4. localStorage separado por ruta base
   - Padre (/main): fmc_main_token, fmc_main_username
   - Demo (/demo): fmc_demo_token, fmc_demo_username
   - Las sesiones no se mezclan

5. Login sin redirect
   - window.location.reload() en vez de window.location.href = '/'
   - Mantiene la URL actual (ej: /demo/ o /main/)

6. Logout sin redirect
   - Solo logout(), sin window.location.href
   - App.tsx muestra login cuando isAuthenticated es false

7. Error del POS al lado del boton + progreso
   - Mensaje de instalacion dentro de cada tarjeta
   - Spinner + "Instalando..." en el boton
   - Logs de instalacion en area expandible

8. Queries del demo filtran por __LOCAL__
   - listDemoUsers: AND u.node_domain = __LOCAL__
   - demoLogin: AND node_domain = __LOCAL__

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
