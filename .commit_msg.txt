fix: Demo no muestra setup - DemoReset no siembra, DemoAutoSetup no borra

## Problema
El demo mostraba la pagina de setup inicial en vez del login con cuentas.

## Causa
El flujo era:
1. DemoReset → borra todo → llama DemoSeedData (siembra todo)
2. DemoAutoSetup → borra todo otra vez → crea solo admin
3. DemoSeedData → corre de nuevo (algunos datos se pierden)

DemoAutoSetup tenia su propia limpieza (lineas 22-78) que borraba
todo lo que DemoReset acababa de sembrar.

## Solucion
1. DemoReset: ahora SOLO borra, no llama DemoSeedData al final
2. DemoAutoSetup: removida la limpieza (ya la hace DemoReset)
3. Flujo correcto:
   - DemoReset: borra todo del dominio demo
   - DemoAutoSetup: crea admin + node_config (initialized=true)
   - DemoSeedData: siembra usuarios, productos, transacciones, etc

## Resultado
- El demo arranca con node_config.initialized=true
- setup/status devuelve initialized=true
- El frontend muestra login con cuentas, no setup
- Todas las cuentas demo aparecen para seleccionar

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
